# This MyClient uses GETS Capable command to retrieve the all capable servers that the job can be scheduled and get the first available server to schedule the jobs.

# Algorithm used
-> FC (First Capable)